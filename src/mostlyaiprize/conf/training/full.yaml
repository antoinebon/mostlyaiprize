# @package _global_  
training:
  # model: "MOSTLY_AI/Large"
  model: "MOSTLY_AI/Medium"
  max_training_time: 180  # (in minutes)
  max_epochs: 2000
  batch_size: 512
  # batch_size: 4096
  # batch_size: 8192
  gradient_accumulation_steps: null
  max_sample_size: null
